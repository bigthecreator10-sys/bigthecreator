const {init}=require("./_db");module.exports=async(req,res)=>{if(req.method!=="POST")return res.status(405).json({error:"Method not allowed"});try{const p=await init(),{name,email,service,date,notes=""}=req.body||{};if(!name||!email||!service||!date)return res.status(400).json({error:"Please complete all required fields."});await p.query("INSERT INTO bookings(name,email,service,date,notes) VALUES($1,$2,$3,$4,$5)",[name,email,service,date,notes]);res.status(201).json({message:"Booking request received. We will contact you shortly."})}catch(e){res.status(500).json({error:"Could not save booking."})}};