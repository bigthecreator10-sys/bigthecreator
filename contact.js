const {init}=require("./_db");module.exports=async(req,res)=>{if(req.method!=="POST")return res.status(405).json({error:"Method not allowed"});try{const p=await init(),{name,email,phone="",message}=req.body||{};if(!name||!email||!message)return res.status(400).json({error:"Please complete the required fields."});await p.query("INSERT INTO contacts(name,email,phone,message) VALUES($1,$2,$3,$4)",[name,email,phone,message]);res.status(201).json({message:"Enquiry received. Thank you."})}catch(e){res.status(500).json({error:"Could not save enquiry."})}};